# 添加 cookie 依赖

## ✅ 已修复

**已在 `package.json` 中添加缺失的依赖**：
- `cookie`: `^0.6.0`

## 🔍 问题原因

代码中使用了 `cookie` 包（在 `server/_core/sdk.ts` 中），但 `package.json` 中没有这个依赖。

## 🚀 立即操作

**在 PowerShell 中运行**：

```powershell
# 安装新添加的依赖
cd d:\tcm-bti-assessment
pnpm install

# 等待安装完成

# 重新启动后端服务器
pnpm dev
```

---

## 📋 完整操作步骤

### 步骤1：安装依赖

```powershell
pnpm install
```

### 步骤2：启动后端服务器

```powershell
pnpm dev
```

### 步骤3：确认后端运行

等待看到：
```
Server running on http://localhost:3000/
```

---

**请先运行 `pnpm install` 安装依赖，然后重新运行 `pnpm dev`！**
