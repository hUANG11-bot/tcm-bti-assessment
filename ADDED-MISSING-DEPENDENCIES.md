# 已添加缺失的运行时依赖

## ✅ 已修复

**已在 `package.json` 的 `dependencies` 中添加以下运行时依赖**：

1. ✅ `@trpc/server` - tRPC 服务器
2. ✅ `@trpc/client` - tRPC 客户端
3. ✅ `@trpc/react-query` - tRPC React Query 集成
4. ✅ `express` - Express 框架
5. ✅ `drizzle-orm` - 数据库 ORM
6. ✅ `mysql2` - MySQL 驱动
7. ✅ `cookie-parser` - Cookie 解析
8. ✅ `dotenv` - 环境变量
9. ✅ `bcryptjs` - 密码加密
10. ✅ `axios` - HTTP 客户端
11. ✅ `jose` - JWT 处理
12. ✅ `nanoid` - ID 生成

---

## 🚀 下一步

### 步骤1：提交修改到 GitHub

**需要提交两个文件**：
1. `package.json`（已添加依赖）
2. `Dockerfile`（已移除 --production）

**使用 GitHub 网页编辑**（最简单）：

1. **访问**：https://github.com/HUANG11-bot/tcm-bti-assessment/blob/main/package.json
2. **点击 "Edit" 按钮**
3. **找到 `dependencies` 部分**
4. **添加以下内容**（在 `"@hookform/resolvers"` 之后）：

```json
    "@trpc/server": "^11.6.0",
    "@trpc/client": "^11.6.0",
    "@trpc/react-query": "^11.6.0",
    "axios": "^1.12.0",
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.2",
    "drizzle-orm": "^0.44.5",
    "express": "^4.21.2",
    "jose": "6.1.0",
    "mysql2": "^3.15.0",
    "nanoid": "^5.1.5",
```

5. **提交更改**

---

### 步骤2：重新部署

1. **在微信云托管控制台**
2. **点击"重新部署"**
3. **等待构建完成**

---

## 📋 修改说明

**为什么需要添加这些依赖**：

- 这些包在代码中被使用，但不在 `package.json` 的 `dependencies` 中
- 虽然 `pnpm-lock.yaml` 中有，但 `package.json` 中没有明确声明
- 为了确保 Docker 构建时正确安装，需要明确添加到 `dependencies`

---

## ✅ 现在请

1. **提交修改后的 `package.json` 到 GitHub**
2. **重新部署**
3. **查看结果**

**完成后告诉我结果！**
