# 如何在 GitHub 上传文件

## 🔍 找到上传文件的方法

在 GitHub 仓库页面，上传文件的方法可能不太明显，以下是几种方法：

---

## ✅ 方法1：通过 "uploading an existing file" 链接

在您的仓库页面，找到 **"Quick setup"** 部分，里面有文字：

> "Get started by **creating a new file** or **uploading an existing file.**"

1. **点击 "uploading an existing file"**（这是一个链接）
2. 会进入文件上传页面

---

## ✅ 方法2：直接访问上传页面

**直接在浏览器地址栏输入**：

```
https://github.com/HUANG11-bot/tcm-bti-assessment/upload
```

这会直接打开文件上传页面。

---

## ✅ 方法3：通过创建文件页面

1. **在仓库页面，点击 "creating a new file"**（在 Quick setup 部分）
2. **或直接访问**：`https://github.com/HUANG11-bot/tcm-bti-assessment/new/main`
3. **在页面底部，有 "Choose your files" 或拖拽区域**
4. **可以上传文件**

---

## 📋 上传文件的步骤

### 步骤1：进入上传页面

使用上述任一方法进入上传页面。

### 步骤2：创建目录结构

**需要先创建目录，然后上传文件**：

1. **创建 `.github` 目录**：
   - 在文件名输入框输入：`.github/`（注意末尾的斜杠）
   - 点击页面上的按钮创建目录
   - 或直接在上传时输入完整路径

2. **创建 `.github/workflows` 目录**：
   - 输入：`.github/workflows/`

### 步骤3：上传文件

**方法A：逐个上传**

1. **上传 `.github/workflows/docker-build.yml`**：
   - 点击 "Choose your files" 或拖拽文件
   - 选择文件：`d:\tcm-bti-assessment\.github\workflows\docker-build.yml`
   - 文件名会自动设置为：`.github/workflows/docker-build.yml`

2. **上传 `Dockerfile`**：
   - 选择文件：`d:\tcm-bti-assessment\Dockerfile`
   - 文件名：`Dockerfile`（根目录）

3. **上传 `package.json`**：
   - 选择文件：`d:\tcm-bti-assessment\package.json`
   - 文件名：`package.json`

4. **上传 `dist/index.js`**：
   - 需要先创建 `dist` 目录
   - 选择文件：`d:\tcm-bti-assessment\dist\index.js`
   - 文件名：`dist/index.js`

5. **上传 `server/` 目录**：
   - 需要逐个上传 `server/` 目录下的所有文件
   - 或使用 Git 命令（如果可用）

6. **上传 `shared/` 目录**：
   - 需要逐个上传 `shared/` 目录下的所有文件

**方法B：使用拖拽（推荐）**

1. **打开文件资源管理器**，导航到：`d:\tcm-bti-assessment`
2. **在 GitHub 上传页面**，直接拖拽以下文件/文件夹：
   - `.github` 文件夹
   - `Dockerfile`
   - `package.json`
   - `dist` 文件夹
   - `server` 文件夹
   - `shared` 文件夹

### 步骤4：提交

1. **在页面底部**，输入提交消息：`Add Docker build workflow`
2. **点击 "Commit changes"**

---

## 🚀 更简单的方法：使用 GitHub Desktop

**如果上传文件太麻烦，可以使用 GitHub Desktop**：

1. **下载 GitHub Desktop**：https://desktop.github.com/
2. **安装并登录**
3. **添加仓库**：
   - File → Clone Repository
   - 选择 `HUANG11-bot/tcm-bti-assessment`
4. **复制文件**：
   - 将项目文件复制到本地仓库目录
5. **提交并推送**：
   - 在 GitHub Desktop 中提交
   - 点击 "Push origin"

---

## 💡 最快的方法：只上传关键文件

**如果上传所有文件太麻烦，可以先只上传关键文件测试**：

### 最小文件集（用于测试 Dockerfile）

1. **`.github/workflows/docker-build.yml`** - GitHub Actions 工作流
2. **`Dockerfile`** - Docker 配置文件
3. **`package.json`** - 依赖配置
4. **`dist/index.js`** - 编译后的入口文件
5. **`server/` 目录** - 服务器代码
6. **`shared/` 目录** - 共享代码

**上传这些文件后，就可以触发 GitHub Actions 测试 Dockerfile 了！**

---

## ✅ 推荐操作

**建议**：

1. **先尝试方法1**：点击 "uploading an existing file" 链接
2. **如果找不到，使用方法2**：直接访问 `https://github.com/HUANG11-bot/tcm-bti-assessment/upload`
3. **或使用 GitHub Desktop**（最简单）

---

## 📝 上传后的步骤

上传文件后：

1. **点击 "Actions" 标签**
2. **查看是否有 "Docker Build Test" 工作流**
3. **如果没有自动运行，手动触发**：
   - 点击 "Actions" 标签
   - 选择 "Docker Build Test"
   - 点击 "Run workflow"

---

## ✅ 现在开始

**请尝试以下任一方法**：

1. **点击页面上的 "uploading an existing file" 链接**
2. **或直接访问**：`https://github.com/HUANG11-bot/tcm-bti-assessment/upload`
3. **或使用 GitHub Desktop**

**完成后告诉我结果！**
